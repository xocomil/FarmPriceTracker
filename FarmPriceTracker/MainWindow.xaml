<Window x:Class="FarmPriceTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:FarmPriceTracker.Controls"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Title="Farm Price Tracker - Main" Height="450" Width="800" Padding="1">
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Close" Executed="CommandBinding_OnExecuted" />
    </Window.CommandBindings>
    <DockPanel>
        <controls:Header DockPanel.Dock="Top" />
        <StatusBar DockPanel.Dock="Bottom" Height="35" />
        <Grid>
            <TabControl>
                <TabItem Header="Fill Types" />
                <TabItem Header="Settings" />
            </TabControl>
        </Grid>
    </DockPanel>
</Window>
